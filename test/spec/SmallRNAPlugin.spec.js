require([
    'dojo/_base/declare',
    'dojo/_base/array',
    'JBrowse/Browser',
    'SmallRNAPlugin/View/Track/smAlignments'
    ], function( 
        declare, 
        array,
        Browser,
        smAlignments
    ) {
    
    describe( 'Init test', function() {
        var test = true;
        it('test', function() {
            expect(test).toBe(true);
        });
    });

});

